// test script for protorabbit client
if ( typeof protorabbit !== 'undefined' ) {
    protorabbit.log( "Testing 3." );
} else if ( typeof console !== 'undefined' ){
    console.log( "Testing 3." );
}
